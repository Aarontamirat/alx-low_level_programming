#include <stdio.h>
/**
 * main - prints the sentence with proper grammer, but the outcome is a piece of art
 * Return: 0 if exists properly, non zero otherwise
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
