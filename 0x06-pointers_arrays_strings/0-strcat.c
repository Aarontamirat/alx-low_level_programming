#include "main.h"

/**
 * _strcat - concatenates two strings.
 * @dest: passed string array.
 * @src: passed string array.
 * Return: concatenated value.
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
