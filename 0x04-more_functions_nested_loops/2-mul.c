#include "main.h"

/**
 * mul - multipliestwo integers
 * @a : is the first number
 * @b : is the second number
 * Return: 0 up on success
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
